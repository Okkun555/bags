export type Occupations = Array<Occupation>;

export type Occupation = {
  id: number;
  name: string;
};

export type Prefectures = Array<Prefecture>;

export type Prefecture = {
  id: number;
  name: string;
};
